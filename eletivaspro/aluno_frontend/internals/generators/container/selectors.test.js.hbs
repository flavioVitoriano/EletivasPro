/**
 *
 * Tests for {{>containerName}} selectors
 *
 */

// import { select{{>containerName}}Domain } from '../selectors';

describe('select{{>containerName}}Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
