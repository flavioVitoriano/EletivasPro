/**
 *
 * {{>containerName}} reducer
 *
 */

import produce from 'immer';
import { DEFAULT_ACTION } from './constants';

export const initialState = {};

const {{>propertyName}}Reducer = produce((draft, action) => {
  switch (action.type) {
    case DEFAULT_ACTION:
      break;
    default:
  }
}, initialState);

export default {{>propertyName}}Reducer;
